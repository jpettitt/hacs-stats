# Wrangler local state — D1 SQLite, KV, Queue simulators.
# Intentionally NOT auto-deleted: local data must survive restarts
# (see ARCHITECTURE.md → Local development).
.wrangler/

# Local secrets (GITHUB_TOKEN, etc.). Commit `.dev.vars.example` instead.
.dev.vars
.dev.vars.*
!.dev.vars.example

# Node / pnpm
node_modules/
.pnpm-store/
.pnpm-debug.log*
npm-debug.log*

# Build output
dist/
build/
.next/
.output/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.recommended.json
*.swp
*.swo

# Test / coverage
coverage/
*.lcov
.nyc_output/

# TypeScript
*.tsbuildinfo

# Env
.env
.env.*
!.env.example
