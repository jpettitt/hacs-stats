{
  "name": "hacs-stats",
  "version": "0.0.0",
  "private": true,
  "description": "Unofficial usage stats for HACS — Home Assistant Community Store.",
  "author": "John Pettitt",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "dev:web": "pnpm --filter @hacs-stats/web dev",
    "dev:scraper": "pnpm --filter @hacs-stats/scraper dev",
    "migrate": "tsx scripts/migrate.ts",
    "seed": "tsx scripts/seed.ts",
    "discover": "tsx --env-file-if-exists=.env scripts/discover.ts",
    "discover:bands": "tsx --env-file-if-exists=.env scripts/discover-bands.ts",
    "backfill:queue": "tsx --env-file-if-exists=.env scripts/backfill-queue.ts",
    "backfill:release-titles": "tsx --env-file-if-exists=.env scripts/backfill-release-titles.ts",
    "sweep:queue": "tsx --env-file-if-exists=.env scripts/sweep-queue.ts"
  },
  "dependencies": {
    "@hacs-stats/db": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.5",
    "tsx": "^4.20.3",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  }
}
